# See https://wiki.hyprland.org/Configuring/Monitors/
monitor=,preferred,auto,auto


# See https://wiki.hyprland.org/Configuring/Keywords/ for more
env = HYPRCURSOR_SIZE,10

#execs
exec-once=waybar
exec-once=hyprpaper

#windowrules
windowrule = opacity 0.9 0.9,class:(.*)(ghostty)

# For all categories, see https://wiki.hyprland.org/Configuring/Variables/
input {
    kb_layout = pl
    kb_variant =
    kb_model =
    kb_options =
    kb_rules =

    follow_mouse = 2

    touchpad {
        natural_scroll = no
    }

    sensitivity = 0

    repeat_rate = 30
    repeat_delay = 175
}

general {
    gaps_in = 5
    gaps_out = 10
    border_size = 1
    col.active_border = rgba(89b482ff)
    col.inactive_border = rgba(d4be98ff)

    layout = dwindle
}

decoration {
    rounding = 0
    
    blur {
        enabled = false
    }
}

animations {
    enabled = no
}

dwindle {
    # See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
    pseudotile = yes # master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = yes # you probably want this
}

# Example per-device config
# See https://wiki.hyprland.org/Configuring/Keywords/#executing for more
device {
    name = steelseries-steelseries-prime
    sensitivity = -2.5
}

misc {
    middle_click_paste = false
    disable_splash_rendering = false
    disable_hyprland_logo = true
    force_default_wallpaper = 0
    background_color = 0x1d2021
}

# See https://wiki.hyprland.org/Configuring/Keywords/ for more
$mainMod = SUPER

# Main binds
bind = $mainMod, RETURN, exec, $TERM
bind = $mainMod, W, exec, $BROWSER
bind = $mainMod, Q, killactive, 
bind = $mainMod, F, fullscreen, 
bind = $mainMod SHIFT, F, togglefloating, 
bind = $mainMod, D, exec, wofi -a
bind = $mainMod, F12, exec, $TERM -e nmtui
bind = $mainMod, m, exec, $TERM -e btm
bind = $mainMod, a, exec, $TERM -e ncpamixer

bind = $mainMod SHIFT, o, exec, hyprlock
bind = $mainMod, x, exec, reboot
bind = $mainMod SHIFT, x, exec, shutdown -h now

# Move focus with mainMod + shift + hjkl keys
bind = $mainMod, H, movefocus, l
bind = $mainMod, J, movefocus, d
bind = $mainMod, K, movefocus, u
bind = $mainMod, L, movefocus, r

# Move window with mainMod + shift + hjkl keys
bind = $mainMod SHIFT, H, movewindow, l
bind = $mainMod SHIFT, J, movewindow, d
bind = $mainMod SHIFT, K, movewindow, u
bind = $mainMod SHIFT, L, movewindow, r

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspacesilent, 1
bind = $mainMod SHIFT, 2, movetoworkspacesilent, 2
bind = $mainMod SHIFT, 3, movetoworkspacesilent, 3
bind = $mainMod SHIFT, 4, movetoworkspacesilent, 4
bind = $mainMod SHIFT, 5, movetoworkspacesilent, 5
bind = $mainMod SHIFT, 6, movetoworkspacesilent, 6
bind = $mainMod SHIFT, 7, movetoworkspacesilent, 7
bind = $mainMod SHIFT, 8, movetoworkspacesilent, 8
bind = $mainMod SHIFT, 9, movetoworkspacesilent, 9
bind = $mainMod SHIFT, 0, movetoworkspacesilent, 10

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

# Screenshots

# Screenshot a window
bind = $mainMod, PRINT, exec, hyprshot -m window output --clipboard-only
# Screenshot a monitor
bind = , PRINT, exec, hyprshot -m output output --clipboard-only
# Screenshot a region
bind = $mainMod SHIFT, PRINT, exec, hyprshot -m region output --clipboard-only
